
// Insert and Remove

// var arr = [ 1, 2, 3, 4 , 5, 6, 7,8]

// [3] = 9
// arr.push(9) // push method add element/data at end
// arr.unshift(9) //unshift methos add the element/data at the first 

// arr.splice( 5, 1, 10,12 ) // Splice use for both add and remove (index no,how many wants to delete, add the data)

// arr.pop() // pop method remove the last data
// arr.shift() // shift method remove the first data

// arr.splice( 3, 2,23, 44)

// console.log(arr)